# 猜☆拳☆王 — 開發路線圖與日誌

> 本文件是這個專案的「共用大腦」:記錄目前做到哪、接下來要做什麼。
> 開發者會協同多個 AI 助理進行作業,**每個 AI 在動工前應該先讀這份文件**,
> 完成一批有意義的工作後,也應該回來更新「開發日誌」與「路線圖」兩節,
> 讓下一個接手的人(不管是人還是 AI)不用重新爬一次 git log 或聊天記錄。

---

## 專案速覽

- **遊戲**:猜☆拳☆王 — 以「剪刀石頭布」為核心,疊加太陽卡(進化)、月亮卡(偷改出招/反制)的雙人消耗戰卡牌遊戲。
- **規則規格書**:[`RPS_Card_Game_Design.md`](RPS_Card_Game_Design.md)(完整規則、勝負表、平衡筆記,規則異動請先改這份文件再改程式)。
- **兩套實作,規則邏輯必須同步**:
  - Python:`rps_core.py`(規則引擎)+ `rps_game.py`(Tkinter GUI)+ `rps_text.py`(終端機版)
  - Web:`web/rules.js` + `web/engine.js`(規則引擎)+ `web/app.js`(前端渲染/互動)+ `web/net.js`(Firebase 連線)
  - **規則或 AI 決策邏輯異動 → 兩邊都要改。純 UI/美術/動畫/文字精簡 → 只改 web 端即可**,Python 端是本機 Tkinter GUI,互動模型不同,不需要跟著做動畫類的東西。
- **部署**:web 版透過 GitHub Actions 自動部署到 GitHub Pages,網址 `roomatef.github.io/mora_duel_king`,repo 為 `github.com/RooMateF/mora_duel_king`。
  - `web/index.html` 的 4 個本地 JS 檔 + `style.css` 都用 `?v=N` 做快取破壞,**改了 app.js/engine.js/rules.js/net.js/style.css 任何一個,記得把 `?v=` 數字一起遞增**,不然玩家瀏覽器可能吃到部署前的舊快取。
- **卡牌美術產生器**:目前用 Python + Pillow 程式化產生印刷卡風格的 PNG 卡圖(`cardgen.py`,目前只存在協作 session 的 scratchpad,尚未進 repo — 見下方「待辦」)。

---

## 開發日誌

> 由舊到新排列。每一段是一個有主題的工作階段,不是逐筆 commit 流水帳。

### 2026-07-31 — 專案啟動、核心規則、第一版 UI
- 初版 commit:Python(桌面 GUI + 終端機)+ Web(Firebase 雙人連線 / 單人 vs AI)雙實作同時建立。
- 修掉幾個 AI 決策 bug(自我犧牲式降級出招、日蝕反制烈陽的觸發率太低）。
- 太陽卡「烈陽」機制重新設計,更名專案為「猜☆拳☆王」。
- 第一版卡牌美術上線(手刻 SVG 線稿風),效果文字改成類似 OCG 的①②編號寫法。
- Web 版从純文字選單改成點卡出牌、支援拖曳出牌 + 按鈕確認、戰場欄位改用真的卡圖渲染。

### 2026-08-01 — 互動體驗大改版
- 出牌流程改成「點卡標記 + 按鈕確認」,拿掉容易誤觸的雙擊出牌。
- 遊戲畫面改成滿版鎖定視窗高度(dvh 單位換算),不再是會捲動的一般網頁。
- 長按卡片彈出放大卡片預覽(仿 Duel Links)。
- 加入開場硬幣動畫(決定先攻)、戰鬥對撞特效(翻牌/太陽強化/月亮發動/勝負對撞)、抽牌動畫、星星被奪走的吸收動畫。
- 自己/對手面板改成共用的 6 格網格版面,支援向下滑動抽牌手勢。

### 2026-08-02 — 畫面同步 bug 修復
- 修掉好幾個「引擎狀態已經寫回去,但畫面還沒重畫」的競態 bug(月亮蓋牌、星星揭示都中過)。
- 修法後來收斂成固定模式:`ui.ask()` 新增 `onResolved` callback,讓引擎在觸發重畫**之前**先把選擇寫回 `Player` 物件。
- `index.html` 開始用 `?v=N` 做腳本快取破壞,避免玩家吃到部署前的舊版 JS。
- 修正「不管先後攻,對手的星星卡看起來都已經蓋好了」的顯示錯誤。

### 2026-08-03 — 卡牌美術從 SVG 全面轉成 Python/Pillow 程式生成(三輪迭代)+ 動畫全面翻新 + 出牌順序修正
這是目前為止最大的一次改版,起因是使用者認為原本手刻 SVG 的美術「簡陋到沒邊」,要求全面重做。

- **動畫翻新**:星星揭示改成真的雙面 3D 翻牌;勝負對撞加上衝擊波 + 戰場震動,烈陽/日蝕發動額外加對應主題色衝擊波;抽牌儀式加光暈;對手出牌飛入改成拱形弧線軌跡;硬幣動畫加環境光暈。
- **美術第一輪**:改用 Pillow 產生遊戲王風格印刷卡(金屬浮雕邊框、漸層名條、屬性圓標、內凹卡圖框)。使用者回饋:太卡通、太多光澤,沒有「質感」。
- **美術第二輪**:邊框/名條/圓標改成霧面拉絲金屬材質(雜訊顆粒+拉絲紋理),圓標從玻璃球感改成刻花徽章;插畫加深(石頭分面陰影、布加纖維紋、剪刀刀刃改成有實際面積的鋼刃);卡背拿掉所有文字,月亮卡背保留新月圖示;場地加上可平鋪的菱格壓紋材質(`web/img/board_texture.png`)。
- **出牌順序修正**:星星蓋牌原本永遠是 p1 先蓋(跟先後攻無關),改成照真正的先攻/後攻順序進行;電腦先攻時會先秀「OOO(難度)的回合」橫幅、演示蓋牌動畫,玩家才輪到出牌。公開快照新增 `starCommitted` 是非旗標(只公開有沒有蓋牌,不洩漏蓋了什麼)。
- **卡牌效果文字精簡**:剪刀石頭布只寫贏什麼;太陽升級卡拿掉贏平輸的括號說明;月亮卡拿掉「需事先蓋放」(月亮卡規則本來就要蓋放才能發動)。三處(`rules.js` / `rps_core.py` / `cardgen.py`)同步。
- **美術第三輪**(目前最新狀態):字體從標楷體/細明體/正黑體全面換成 Yu Gothic(系統內唯一風格夠不同的黑體),卡名加描邊+陰影做成遊戲 Logo 式處理。11 張卡插畫全部重畫:石頭拿掉玻璃高光、布改柔軟垂墜輪廓(鈦合金布額外加銳利硬邊)、剪刀改斜角開闔的動態帥氣姿勢(順便修掉一個刀刃幾何是退化四邊形、幾乎沒有面積的舊 bug)、殞石頭改成宇宙墜落+大氣摩擦火焰的獨立場景、烈陽改成天空+太陽+乾裂大地場景、日蝕沿用同場景加咬向太陽的漫畫式狼頭剪影(天狗食日)、偷變石頭改成驚喜箱彈出彈簧拳頭、偷變剪刀/布維持魔術師的手但加袖口+火花(布另加煙霧)。整體遊戲畫面加背景光暈、立體漸層按鈕、面板陰影邊框、回合數改徽章樣式。

> 詳細技術細節(每個 bug 的根因、Pillow 踩過的坑)記在協作 session 的長期記憶裡,搜尋關鍵字「reference_rps_card_game_project」。

### 2026-08-04 — cardgen.py 進版控 + 外部 AI 圖檔整合流程 + 規則修正
- `tools/cardgen.py`:卡牌美術產生器正式進 repo(先前只存在協作 session 暫存區)。
- 新增 `generate_from_external_art()` / `python cardgen.py --external`:讀 `tools/cardgen_source_art/` 裡跟卡名同名的外部圖檔(AI 生成或委託繪製),用 cover-fit 裁切塞進既有的金屬邊框/名條/圓標/效果文字框,取代程式內建插畫;找不到圖檔的卡自動退回內建插畫。`tools/cardgen_source_art/README.md` 附 11 張卡的統一風格錨點與個別 prompt。**目前資料夾還是空的,等使用者生完圖丟進去。**
- **規則修正**:太陽升級卡(殞石頭/雷射剪刀/鈦合金布)型別不符時,原本實作是「收回手牌、下回合可再打」,跟 `RPS_Card_Game_Design.md` 原始設計(直接進棄牌區)不一致 —— 已修正為「視為發動無效並丟棄」,`engine.js`/`rps_core.py`/`rules.js`/`cardgen.py`/`CARD_EFFECTS.md` 同步。
- ~~待辦:`殞石頭`/`雷射剪刀`/`鈦合金布` 圖上效果文字是舊版~~ → 已在下方這輪外部 AI 美術整合時一併帶新文字重新生成。

### 2026-08-04(第三次)— AI 改成「圍繞太陽卡佈局」,升級卡不再整局出不掉
使用者回報:困難電腦常常整局結束太陽牌都沒出到。查下來是決策順序反了 —— `aiChooseStar()`
只讀對手的出招習慣挑星星,完全沒看自己手上的太陽卡;要等到太陽階段的 `aiDecideSun()` 才回頭
檢查「剛剛那張星星剛好能不能升級」。只要讀牌結果跟升級卡型別對不上,升級卡就永遠是廢牌。

- 改成先看手上的升級太陽卡(`aiStarForSunPlan()`),如果它需要的星星還有存貨,就優先蓋那張星星
  讓太陽卡這回合能發動;有多張可選時,挑「升級後打得贏預測出招」的那張,其次是能打平的。
- 照難度給不同的執行率(easy 0.3 / normal 0.75 / hard 0.92),簡單模式維持大多隨便出,保住難度落差。
- 抽出 `predictHumanStar()`,讓讀牌預測同時給「挑升級卡」跟「反制對手」兩邊共用。
- **`rps_core.py` 已同步**(`ai_star_for_sun_plan` / `predict_human_star`),兩邊 300 局模擬數據吻合。
- 300 局 A/B 模擬(對手隨機出牌,勝率絕對值會偏高,看相對差異即可):

  | | AI 勝率 | 平均用掉升級卡 | 局末還卡在手上 |
  |---|---|---|---|
  | 舊版 normal | 70.3% | 1.59 | 0.90 |
  | 新版 normal | 76.3% | 2.33 | **0.11** |
  | 舊版 hard | 81.3% | 1.39 | 0.94 |
  | 新版 hard | 86.7% | 2.23 | **0.07** |

  「升級卡卡在手上出不掉」從 ~0.9 張降到 ~0.1 張,使用者回報的問題基本解決。
- 附帶效果:太陽卡是在星星揭示**之前**打出的,所以照太陽卡佈局等於預告了自己的星星型別。
  這不是 bug —— 對手可以用月亮偷變來反制(偷變成石頭跟雷射剪刀打平之類),屬於設計上本來就有的攻防。

### 2026-08-04(第二次)— 全部卡面/卡背換成外部 AI 生成美術,場地初步重新蒙皮
使用者自己用 AI 工具生成了一整套卡面(11 張,完整框線+插畫+文字都畫好了,不是只有插畫本體)、
兩種風格的卡背(油畫感 / 2D 平面感,選了 2D 版)、以及 3 張戰場介面設計圖,放進 `card_game_png/`
資料夾(**未進 git**,46MB 太大,已加進 `.gitignore`,只留在本機當參考)。

- 卡面/卡背檔名原本因為編碼問題變成亂碼(UTF-8 位元組被誤判成 cp437 又存回去),已用
  `name.encode('cp437').decode('utf-8')` 還原成正確中文檔名。
- 逐張核對卡面上「烤」進去的效果文字,跟目前 `CARD_EFFECTS.md` 的規則文字**完全一致**(包含最新的
  太陽卡型別不符規則),不需要額外修圖。
- 卡面(750×1050,比例剛好符合網頁端 400:560)直接沿用;卡背(原生 1632×2176,比例 0.75)置中
  裁成 400:560 再縮到 800×1120,全部存回 `web/cards/`,取代 Pillow 生成版。
- `web/cards/*.png` 檔名不會變、但內容整批換掉 → 新增 `app.js` 的 `CARD_ASSET_VERSION` 常數,
  `cardImgSrc()` 一併加上 `?v=`,比照 `index.html` 的邏輯做快取破壞(**下次卡圖內容異動記得遞增**)。
- 戰場介面**已改成照 `battlefield_layout_slots_v2.png` 重做**(2026-08-04)。做法是保留原有的
  `#oppBand / #battlefield / #myBand` 三段式 DOM,把設計圖的視覺語言用 CSS + 內嵌 SVG 疊上去,
  不走切版/九宮格圖片(向量在任何欄位尺寸下都保持銳利,色彩也集中在一處好維護):
  - 背景直接用設計稿空版 `battlefield_base_empty_v2.png`(`web/img/board_base_v2.jpg`,
    全解析度 JPEG 壓到 204KB)。舊的 480px 縮圖版 `board_bg.jpg` 與菱格壓紋 `board_texture.png` 已移除。
  - 戰場兩排**鏡像對稱**:對手排 `月/星/日`、我方排 `日/星/月`,星星永遠在正中間對撞。
  - **分區配色**:靠 `data-side="opp"/"mine"` 驅動,對手側(上)壓暗成近黑藍、我方側(下)提亮成鋼藍。
  - **空欄位 = 天體卡框**:卡框漸層底 + 置中的太陽/星星/月亮 symbol 浮水印 + 同心橢圓、四邊菱形、
    邊角刻痕。symbol 與框裝飾都由 `app.js` 的 `SLOT_SYM` / `FRAME_DECO` 產生內嵌 SVG。
    太陽欄位另外給金色邊框(設計圖裡唯一被金框起來的格子)。
  - **星星庫存列**:一律顯示拳頭/手掌/剪刀「底座」卡框(`socket_*.png` 深色版給對手、
    `socket_*_blue.png` 藍色版給我方),底部加上設計圖的 ◇◇◇ pips。
    注意 pips 只畫 3 顆,但星星會因為對戰吸收而超過 3 張,所以**數字 badge 一律保留**、pips 純粹當視覺呼應。
  - 牌庫用多層位移陰影做出「一疊牌露出紙邊」的堆疊感,不額外增加 DOM。
  - **牌庫改用設計圖專屬的深色牌庫美術**(2026-08-04 追加):從 `battlefield_layout_slots_v2.png`
    切出設計圖的牌庫卡面(炭黑底+金色太陽徽記 / 深藍底+藍色新月徽記),存成
    `web/img/deck_sun.png` / `deck_moon.png`,由 `pileCardTile()` 的 `isDeck` 參數切換。
    **只有牌庫(太陽庫/月亮庫)用這套**;對手的「太陽手牌/月亮手牌」張數指示仍用亮色的華麗卡背 —— 
    設計圖刻意讓牌庫壓得住當背景元件、手上的牌才搶眼。`.pile-deck` 另外取消 `.pile-back` 的
    減亮濾鏡,否則金色徽記會整個沉掉。
  - 特效配色一起對齊:太陽由橘紅改**金色**、月亮由紫改**冷藍**(並移除 `hue-rotate`,
    避免把卡圖本身的色相扭掉),衝擊波/選取提示/按鈕的綠色 accent 全面改金色系
    (按鈕改「深藍底 + 金邊」,因為金底配白字對比不足)。

### 2026-08-08 — 房間清理機制(避免 Firebase 資料/流量無限累積)+ 素材預載

其他協作 session 這段時間陸續做完大絕美術整合、戰鬥碰撞特效、太陽升級變身動畫、
開場硬幣美術、勝負結算畫面、棄牌區查看、大廳重新設計、AI 決策修正、牌庫美術
(詳見上面各段日誌與下方路線圖的打勾項目)。這次額外處理兩件跟連線基礎設施
有關的問題:

- **房間清理**:使用者提問「一直有人開著房間會不會永遠留在資料庫」——答案是會,
  Realtime Database 沒有 TTL,原本沒有任何地方刪房間。三道防線:等待對手期間
  掛 `onDisconnect` 自動刪除(對手一進來就取消,避免對戰中斷線誤刪)、對局結束
  後房主延遲 20 秒刪除、進大廳時掃描刪除超過 24 小時的房間。掃描用獨立的
  `roomIndex` 節點(只存代碼→開始時間),刻意不開放讀取 `rooms` 根節點 ——
  Firebase 規則往下放寬,那樣會連 `private`(雙方手牌)一起洩漏,是作弊漏洞。
  同時把發布的對戰紀錄從「每次都整份重傳」改成只送最後 60 行,原本每次狀態
  更新雙方都要重新下載一次完整紀錄,流量遠大於實際資料量。**這個過程中連帶
  修掉一個潛在 bug**:戰鬥動畫原本靠「log 陣列長度差」判斷新事件,紀錄變成
  固定長度滑動視窗後長度不再遞增,動畫會靜悄悄失效;改成發布累計序號 `logSeq`
  來抓差異。詳見 `web/database.rules.json` 上方的踩雷說明與 `web/net.js` 的
  `armRoomAutoDelete`/`sweepStaleRooms` 等函式。**規則檔已部署到 Firebase 並
  實測生效**(端對端驗證:老化房間確實被清掉、對手讀不到我方手牌、無法列出
  整個 rooms 節點)。
- **素材預載**:使用者反映「畫面特效、卡片圖案比較常有延遲出現」。根因是
  `<img src>`/CSS `background-image` 都只有真的用到才發請求,大絕/碰撞/開場
  硬幣/結算畫面這些「整局第一次觸發才用到」的圖平常完全沒被抓過,事件發生那
  一刻才臨時下載+解碼。新增 `preloadAllImages()`(`web/app.js`),頁面載入時
  就用背景 `Image()` 物件把全部 44 個素材路徑(卡面/卡背 + 複用既有的
  `STAR_SOCKET_SRC`/`IMPACT_TYPE_SRC`/`ULTIMATE_*`/`GO_*` 等路徑常數,沒有
  重複硬寫)抓進快取並 `decode()`,之後事件觸發時直接秒開。已用網路請求記錄
  確認全部 200/304、零 404,2.5 秒內全數載入完成、無破圖。

---

## 長期開發目標(路線圖)

### 目前優先順序(2026-08-04 使用者確認)
> **先把視覺打動人心的部分做好,音效放在所有特效項目的最後一個順位。**
> 也就是說:下面列的視覺/美術/演出類項目都排在音效前面。

**優先度:高 — 視覺衝擊力**
1. **卡背與名條細節密度**:目前卡背只有同心圓+徽章,對比印刷卡通常更花的底紋,還有進一步加工空間(浮水印圖騰、更複雜的邊框雕花)。
2. ~~戰鬥/發動特效的「份量感」+ 開場硬幣~~ → **已解決**(2026-08-07):`EFFECTS_ART_BRIEF.md`
   那 6 張圖生完丟進 `card_game_png/game_effects_assets/`,裁邊去背(alpha 已經內建、不用
   額外去背處理)、壓縮後放進 `web/img/`。
   - **硬幣**:`web/style.css` 的 `.coin-face`/`.coin-back` 改用 `coin_face_sun.png`/
     `coin_face_moon.png` 當背景圖(`background-size:contain`),硬幣本體從 120px 放大到
     156px;玩家名字文字用 `align-items:flex-end` + `padding-bottom` 疊在圖片下半段留白的
     銘牌區,不再是純色圓形。
   - **戰鬥碰撞**:新增 `spawnImpactBurst(targetEl, starType)`(`web/app.js`),依碰撞的星星
     型別疊 `impact_rock.png`/`impact_scissor.png`/`impact_cloth.png` 加上共用的
     `impact_flash.png` 底閃光,疊在既有 `spawnShockwave` 光環上面;贏家型別決定疊哪張,
     平手時兩邊型別必然相同、直接用該型別。
   - **太陽升級「變身」特效補強**:使用者反應原本的變身特效太不明顯,幾乎看不出來。
     `playEvolveEffect()` 大幅加強:新增全螢幕金色暈染(`.evolve-vignette`)先把視線拉過來、
     疊 `impact_flash.png` 當能量灌注閃光、進化卡圖疊圖本體從原尺寸放大到 1.55 倍、
     動畫總長從 1.15s 拉到 1.6s、觸發延遲從 350ms 縮到 250ms(感覺更緊接著太陽卡出牌),
     並加上戰場面板震動。`delayForLogLine()` 也同步把「打出太陽卡:殞石頭/雷射剪刀/鈦合金布」
     這行 log 的停留時間拉長到 1900ms,避免特效播到一半被下一行 log 切斷。
     已用 DOM 直接呼叫 `playEvolveEffect()`/`spawnImpactBurst()` 確認素材路徑、放大後的尺寸、
     `fx-shake` 觸發、CSS keyframe 都正確套用;受限於預覽窗格渲染問題一樣沒能肉眼看動畫播放。
   - **(2026-08-07 使用者實測回饋後追加調整)**:動畫時間太短、感受不到就結束了 → 全面拉長:
     `impact-burst-flash` 0.4s→0.7s、`impact-burst-type` 0.65s→1.3s、勝負對撞這行 log 的
     `delayForLogLine` 停留從 550ms 拉到 1400ms;`playEvolveEffect` 的 vignette 0.9s→1.3s、
     flash 0.75s→1.1s、overlay 主體 1.6s→2.3s,升級卡那行 log 的停留從 1900ms 拉到 2650ms;
     開場硬幣結果顯示後的停留從 1850ms 拉到 2700ms。另外修正 `spawnImpactBurst` 的疊圖位置
     bug:型別碰撞美術(石頭碎裂/剪刀劈砍/布纖維噴散)原本疊在**贏家**那張星星卡上,語意錯了
     (應該是「贏家的招式打在輸家身上」),改成疊在 **輸家** 那張星星卡上,贏家維持既有的
     金色勝利光環(`clash-shock-win`)不變。
   - **(2026-08-07 第三批)`ULTIMATES_ART_BRIEF.md` 7 張圖已生成並整合完畢**:
     - **烈陽**:`playBlazingSunEffect()` 疊全螢幕炙陽閃光(`ultimate_sun_flare.jpg`)+ 疊在
       戰場下方、由小長大的地面龜裂(`ultimate_sun_groundcrack.png`),配合既有的橘色衝擊波。
     - **日蝕**:`playEclipseCutin()` 播三格分鏡硬切過場(`eclipse_wolf_lunge/bite/aftermath.jpg`,
       各持續 550/750/550ms),第 2 格(咬穿瞬間)配合戰場震動,播完才接原本的藍色衝擊波
       (不然疊在全螢幕過場底下會被蓋住看不到)。新增 `#eclipseCutinOverlay` 覆蓋層。
     - **偷變系列**:`spawnStealConjurePuff()` 依 `MOON_STEAL[nowMoon]` 判斷,疊紫色施法煙霧
       (`steal_conjure_puff.png`)在月亮卡格上。
     - **速度殘影**:`spawnSpeedStreak()` 整合進 `flyGhost()`(對手出牌飛入動畫的底層函式),
       依起訖點算角度/長度疊一道拖尾(`speed_streak.png`),太短距離(<24px)不畫避免糊成一團。
     - `delayForLogLine()` 同步新增「打出【烈陽】!」(1800ms)、「發動【日蝕】」
       (`ECLIPSE_CUTIN_TOTAL_MS + 300`,約 2150ms)兩個獨立分支,原本共用的「發動【.+】」
       一般分支(1500ms)不變。
     - 已用 DOM 直接呼叫四個新函式確認素材路徑、CSS keyframe 都正確套用,`playEclipseCutin()`
       額外用短延遲(50ms)驗證第一格(撲咬前)確實最先出現,不是卡在最後一格。
3. **場地沉浸感**:目前場地是三個堆疊的矩形面板 + 平鋪材質,離「一張桌子」的感覺還有距離。可考慮加透視感的桌面背景插圖、依卡牌陣營(星/日/月)動態變化的環境光。
4. ~~插畫技法上限~~ → **已解決**(2026-08-04):11 張卡面 + 卡背 + 戰場背景全部換成外部 AI 生成美術,詳見下方「2026-08-04(第二次)」日誌段落。原始素材放在本機 `card_game_png/`(未進 git,46MB 太大)。
5. ~~大廳畫面重新設計~~ → **已解決**(2026-08-06):依 `LOBBY_UI_DESIGN.md` 規範 +
   使用者提供的「STELLAR VEIL」風格參考圖(占星羅盤 Logo、四角細框、右側旋轉星盤、
   薄邊框全息按鈕群)重寫 `web/index.html` 的 `#lobby` 區塊與 `web/style.css` 對應樣式。
   重點:CSS 變數獨立命名(`--lb-*`)避免污染戰場主題色;按鈕改用直向卡片列表
   (建立房間/加入房間/單人對戰難度選擇/規則說明),角色代碼輸入框與 AI 難度按鈕
   內嵌在各自卡片裡;右側星盤用多層 `radial-gradient` + `repeating-conic-gradient`
   疊出同心圓刻度與星點,`animation: 160s linear infinite` 極慢速旋轉,
   `prefers-reduced-motion` 時停用。字體改接 Google Fonts(Cinzel + Noto Serif TC +
   Noto Sans TC),`<head>` 加 `preconnect`。手機寬度(`max-width:760px`)星盤縮小透明度降低、
   單欄置中。所有互動元素 id(`createBtn`/`joinBtn`/`diffBtn`/`nameInput`/`codeInput`/
   `rulesBtnLobby`)維持不變,`app.js` 完全不用改。已用 `read_page`/`getBoundingClientRect`/
   DOM class 切換驗證邏輯正確(手機寬度截圖視覺確認,桌面寬度截圖工具本身有渲染延遲問題,
   改用 DOM 狀態驗證繞過)。
6. ~~勝利/失敗結算畫面~~ → **已解決**(2026-08-07):`ENDSCREEN_ART_BRIEF.md` 那 5 張圖生完
   丟進 `card_game_png/game_endscreen_assets/`,處理後放進 `web/img/`(背景壓成 JPEG 約
   260-410KB、徽章/粒子裁去透明邊界後縮到 640px/240px 寬,alpha 去背用 numpy 驗證過無綠幕
   殘留)。`web/index.html` 新增 `#gameOverOverlay`,`web/style.css` 加 `.go-*` 一組樣式:
   背景淡入 → 徽章彈跳浮現(`goMedallionPop` keyframe)→ 結果文字/獲勝者名/按鈕依序滑入,
   16 顆星芒粒子用 CSS custom properties(`--gp-left/--gp-dur/--gp-delay/--gp-drift`)各自
   跑 `goParticleRise`。`web/app.js` 的 `showGameOver()` 整個重寫:依 `winnerRole===myRole`
   判斷勝/敗套對應圖 + `data-result` 屬性(給 CSS 挑金/藍配色),單人模式才顯示「再戰一次」
   (沿用 `lastDifficulty`),「返回大廳」統一用 `location.reload()` 圖省事。
   驗證方式:因為 Claude Browser 預覽窗格這次卡在「不 compositing frames」的狀態,
   screenshot 一直失敗,改用 DOM/computed-style/network request 三管齊下驗證邏輯正確
   (背景圖 URL、徽章 src、文字內容、按鈕顯隱、圖檔 fetch 200 都確認過),沒能肉眼看到
   動畫本身跑起來,實機測試時请留意動畫時序/粒子效果是否符合預期。
7. ~~太陽/月亮進化變身動畫~~ → **已解決**(2026-08-07):殞石頭/雷射剪刀/鈦合金布這三張
   升級卡命中型別成功發動時(`SUN_EVOLVE[card][0] === 該回合星星卡`),星星欄位現在會播一段
   「變身」特效:一張浮動的進化後卡圖(`playEvolveEffect()`,`web/app.js`)疊在星星欄位上短暫
   閃現、一道從太陽欄位射向星星欄位的金色光束、星星本尊的金色脈衝光暈,外加既有的衝擊波。
   星星欄位實際 DOM 內容不會真的換成進化卡(下一輪 `renderBattlefield` 一樣畫回原本的
   石頭/布/剪刀),這只是短暫的視覺演出,跟結算畫面/既有衝擊波一樣用「算好座標直接貼在
   `document.body` 上」的手法,不受戰場整個重繪(`bf.innerHTML=""`)打斷。日蝕/偷變等
   其他升級判定不動,只在型別相符的升級瞬間觸發。已用 DOM 直接呼叫 `playEvolveEffect()`
   確認 overlay 圖檔路徑/定位、光束座標與角度、pulse class 都正確套用,CSS keyframe 規則
   也確認有進到 stylesheet;因為同一顆預覽窗格 compositing 的限制,一樣沒能肉眼看到動畫。

8. ~~棄牌區查看~~ → **已解決**(2026-08-07,使用者標為「最要緊一定要放進來的功能」):
   之前棄牌區(`chip-discard`)只顯示張數,看不到內容。`boardSnapshotFor()` 新增
   `discard` 欄位(完整卡名陣列,棄牌本來就是雙方公開資訊,不算洩漏隱私手牌);
   雙方的棄牌 chip 現在可以點按(`pileChip()` 新增 `onOpen` 參數),打開
   `#discardOverlay` 用格狀排列列出所有已打出的太陽/月亮卡,每張沿用 `handCardTile()`
   既有的長按看效果文字機制。星星卡不會出現在棄牌區(規則上星星卡本來就不會被丟棄),
   不需要額外處理。已用 DOM 直接操作 `game.p1.discard`/`game.p2.discard` 注入測試資料、
   呼叫 `renderLocalState()` 重繪,確認雙方 chip 都能點開、標題正確、空棄牌區顯示提示文字、
   關閉按鈕正常。

**優先度:中 — 遊戲性與節奏**
9. **減少「無牌可出、一直按跳過」的空轉感**:太陽/月亮階段沒牌時目前還是要跑過整套提示流程,可考慮偵測「雙方皆無牌可出」時直接靜默跳過該階段,不用玩家手動點掉。
10. **勝負進度可視化**:目前畫面上沒有明確的「戰況」指標(只有牌堆數字),可以加一個簡化的「牌庫存量條」或「敗北倒數」視覺化,讓玩家一眼看出誰快輸了。
11. **AI 難度區分度**:簡單/普通/困難目前主要靠決策機率參數調整,可玩測簡單模式是否真的「弱得有存在感」而不是純隨機。

**優先度:低 — 最後處理**
12. **音效**:目前完全靜音。等視覺打磨到一定程度後,再補上翻牌、出牌、衝擊、抽牌、勝利/落敗音效與 BGM。
13. 待玩測後才需要決定的規則細節(見 `RPS_Card_Game_Design.md` 第 11 節):日蝕反制的補償細節、烈陽逼抽是否要限次數、是否要有回合數上限。

### 已解決的技術債
- ~~`cardgen.py` 只存在協作 session 暫存資料夾、沒進版控~~ → 2026-08-04 已移進 `tools/cardgen.py`,並新增 `tools/cardgen_source_art/`(外部插畫圖檔放置處 + prompt 說明文件)。

---

## 給協作 AI 的提醒

- 改規則/AI 決策邏輯 → 記得 `rps_core.py` 跟 `web/rules.js`+`web/engine.js` 兩邊同步,改完用兩邊各自的測試腳本(自己在 session 裡重建,repo 裡沒有存)跑過。
- 改 `web/` 下任何 JS/CSS → `web/index.html` 的 `?v=N` 要遞增。
- 美術/動畫類改動,**一定要在 Claude Browser(或其他瀏覽器自動化工具)裡實際玩過幾回合**,確認沒有 console 錯誤、動畫真的有播出來,再回報完成 —— 這個專案已經好幾次「看起來改對了,實際玩才發現時機/競態沒處理好」。
- 大改動前先問使用者要不要 push;push 前先問一次是這個專案目前的習慣。
- 完成一批工作後,回來更新本文件的「開發日誌」(加一段)跟「長期開發目標」(打勾/移除做完的項目、視情況調整優先順序)。

### ⚠️ `web/database.rules.json` 兩個不能踩的地雷

1. **絕對不要在 `rooms` 這一層加 `.read`。** Firebase 規則是**往下放寬**的:一旦在
   `rooms` 給了讀取權限,底下的 `rooms/$code/private`(雙方的手牌、蓋著的星星卡)
   也會一起開放,對手就能直接讀到你這回合蓋了什麼 —— 這是直接的作弊漏洞,
   而且下層寫得再嚴也擋不住。需要「列出所有房間」時請改用 `roomIndex` 節點
   (只存代碼→開始時間,不含牌面資訊)。
2. **這個檔案不能寫註解。** Firebase 會把不是 `.read`/`.write`/`.validate`/`.indexOn`
   的 key 當成子路徑名稱,所以 `"//": "說明文字"` 會噴
   `Expected '{'`(`//` 不是合法路徑)。要留說明就寫在這裡或 `web/net.js`。

改完這個檔案要**手動部署**才會生效(主控台貼上發布,或 `firebase deploy --only database`)。
repo 裡的檔案只是原始碼,不會自動套用;沒部署時房間清理會拿到 `Permission denied`,
但程式有 catch,只在 console 警告、不影響遊戲。
