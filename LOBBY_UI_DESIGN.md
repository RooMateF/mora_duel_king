# 猜☆拳☆王 — 大廳 UI 設計規範 (Astrology-Tech Minimalist)

> 本文件為**大廳畫面**(`#lobby`,建立房間/加入房間/單人對戰選擇)的視覺設計指導,
> 不是戰場/卡牌美術(那部分見 `ROADMAP.md` 與 `tools/cardgen_source_art/README.md`)。
> 目標風格:「占星 + 科技 + 輕微詭秘」的極簡日漫風,尚未實作,列在 `ROADMAP.md` 的待辦裡。

---

## 1. 色彩規範 (Color Palette)

色彩以深邃的冷色調為主,輔以高對比的發光色作為強調。

| 類別 | 顏色描述 | HEX 色碼 | 用途 |
|---|---|---|---|
| 主背景色 | 深邃星空藍 | `#0A0E1A` | 整個大廳的底層背景 |
| 次背景色 | 幽冥灰藍 | `#161B29` | UI 面板、彈窗底色 |
| 主要邊框 | 冰銀藍 | `#A0C4FF` | 按鈕邊框、裝飾線條 |
| 發光強調 | 恆星金 | `#FFD700` | 標題星星、關鍵選中項、占星盤亮點 |
| 科技光效 | 全息青藍 | `#00F2FF` | 按鈕懸停效果、科技感流光 |
| 文字主色 | 象牙白 | `#F0F0F0` | 主要標題、按鈕文字 |
| 文字次色 | 塵埃灰 | `#8892B0` | 輔助說明文字、未選中項 |

---

## 2. 字體規範 (Typography)

建議使用具有設計感且清晰的黑體,避免使用過於傳統的字體。

- **主標題(LOGO/Game Title)**:建議字體 `Cinzel`(Google Fonts)或 `Noto Serif TC`(粗體)。風格:優雅、帶有古老神祕感。
- **按鈕文字**:建議字體 `Montserrat` 或 `Noto Sans TC`(中黑)。風格:簡潔、現代、科技感。
- **字體大小**:標題 32px–40px;按鈕 18px–22px;腳註/微小裝飾 12px。

> 注意:Google Fonts 需要外部連結,若走 Claude Artifact/離線環境要改成內嵌 `@font-face` data URI;
> 目前 `web/` 的其餘畫面(遊戲內)已改用 Yu Gothic 系統字型,大廳若引入新字體,
> 要確認跟遊戲內畫面切換時風格不會太跳。

---

## 3. 按鈕設計與交互 (Button Interaction)

按鈕採用「薄邊框全息感」設計。

### 3.1 基礎樣式 (Default)
- 背景:`rgba(22, 27, 41, 0.6)`(半透明)
- 邊框:`1px solid #A0C4FF`
- 外圓角:`4px`(輕微圓角)
- 裝飾:角落處有細微的 2D 幾何圖騰或微型觸手線條。

### 3.2 懸停效果 (Hover)
- 邊框顏色轉變為 `#00F2FF`。
- 增加 `box-shadow: 0 0 15px rgba(0, 242, 255, 0.4)` 的外發光效果。
- 文字輕微向右位移 2px。
- 背景透明度略微增加。

### 3.3 點擊效果 (Active/Click)
- 邊框瞬間閃爍為 `#FFD700`(恆星金)。
- 縮放比例 `scale(0.98)`。

---

## 4. 裝飾元素指導 (Decoration)

### 4.1 占星背景 (Astrology Background)
- 星盤線條:使用極細的線條(0.5px),顏色設為 `#8892B0` 且透明度約 0.2。
- 動態感:建議星盤以極慢的速度(如 120s 旋轉一圈)進行旋轉。

### 4.2 克蘇魯元素 (Cthulhu-lite)
- 隱藏圖騰:在螢幕角落或 UI 框架的邊緣,隱藏極細的觸手或深淵之眼符號。
- 出現方式:這些元素不應直接吸引注意力,而是作為背景細節存在。
- 動態:可以加入極其輕微的「呼吸感」動畫(透明度在 0.1 到 0.3 之間緩慢起伏)。

---

## 5. UI 佈局比例 (Layout)

- 垂直居中佈局:核心按鈕群組位於畫面中央偏下位置。
- 黃金比例留白:畫面頂部保留 30% 的空間給標題與星空,增強大氣感。
- 按鈕間距:按鈕之間的垂直間距建議為 24px–32px。

---

*本文件為視覺指導建議,開發者可根據實際網頁框架(CSS/Tailwind)靈活調整數值以達到最佳視覺效果。*
